name: state_change_demo
description: "States and how they affect each other"
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.1.4
  get_it: ^7.7.0
  form_field_validator: ^1.1.0
  flutter_spinkit: ^5.2.1
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.1
  google_maps_flutter: ^2.7.0
  flutter_polyline_points: ^2.1.0
  geopoint: ^1.0.0
  cloud_firestore: ^5.1.0
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  image_picker: ^1.1.2
  firebase_storage: ^12.1.1
  google_fonts: ^6.2.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/Tap & Tell-2.png
